remove dev locally:
git branch -d dev

remove dev remotely:
git push origin : dev

list tags:
git tag --list

delete tag remotely:
git tag -d tagname

delete tag remotely:
git push origin --delete tagname